machine:
  python:
    version: 2.7.11
  hosts:
    database: 127.0.0.1

dependencies:
  pre:
    - pip install -r requirements.txt -r dev_requirements.txt

test:
  override:
    - make test_unit
    - make test_integration
