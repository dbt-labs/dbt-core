name: agents_demo
version: "1.0.0"
config-version: 2

profile: agents_demo

model-paths: ["models"]

models:
  agents_demo:
    +materialized: table
    sales:
      +group: sales
    support:
      +group: support
    finance:
      +group: finance
      +access: private
