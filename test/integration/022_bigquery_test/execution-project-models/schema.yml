version: 2
models:
- name: model
  description: |
    I'm testing the profile execution_project
  tests:
    - project_for_job_id:
        region: region-us
        project_id: {{project_id}}
        job_id: {{job_id}}