# Compile only exercises dialect-specific static analysis / SQL rendering, so
# no warehouse connection is made. The connection values below are unused.
default:
  target: dev
  outputs:
    dev:
      type: redshift
      host: dummy_host
      port: 5439
      user: dummy_user
      password: dummy_password
      database: dummy_database
      schema: dummy_schema
