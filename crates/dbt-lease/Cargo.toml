[package]
name = "dbt-lease"
description = "File-based lease/mutex primitive for gating concurrent access to a shared local file, plus OS-appropriate cache-directory resolution"
version.workspace = true
homepage.workspace = true
repository.workspace = true
authors.workspace = true
license.workspace = true
keywords.workspace = true
edition.workspace = true

[lints]
workspace = true

[dependencies]
base64 = { workspace = true }
dbt-fusion-workspace-hack = { version = "0.1" }
dirs = { workspace = true }
rand = { workspace = true }
tempfile = { workspace = true }
thiserror = { workspace = true }

[lib]
name = "dbt_lease"
path = "src/lib.rs"
doctest = false
