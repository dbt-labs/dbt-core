kind: Features
body: automatically create a latest-version view (e.g. `dim_customers`) for versioned models, opt-in via `latest_version_view_enabled_by_default` flag or per-model `latest_version_view` config
time: 2026-03-30T12:18:43+05:30
custom:
    Author: aahel
    Issue: "7442"
