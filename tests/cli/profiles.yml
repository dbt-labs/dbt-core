jaffle_shop:
  outputs:
    dev:
      type: postgres
      database: postgres
      schema: jaffle_shop
      host: localhost
      user: root
      port: 5432
      password: password
  target: dev
